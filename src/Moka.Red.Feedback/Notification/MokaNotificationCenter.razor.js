/**
 * MokaNotificationCenter JS module. The panel behaves like MokaNotificationBell's, so it shares
 * that module; the relative import resolves against this module's own URL.
 */
export { bindPopup, focusElement, focusNth } from '../NotificationBell/MokaNotificationBell.razor.js';
