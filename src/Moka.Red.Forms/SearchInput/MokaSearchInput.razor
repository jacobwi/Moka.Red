@namespace Moka.Red.Forms.SearchInput
@inherits MokaVisualComponentBase

<div class="@CssClass" style="@CssStyle">
	@if (Label is not null)
	{
		<label class="moka-search-label" for="@InputId">@Label</label>
	}
	<div class="moka-search-wrapper">
		<span class="moka-search-icon" aria-hidden="true">
			<MokaIcon Icon="MokaIcons.Action.Search" SizeValue="14px"/>
		</span>
		<input class="@InputCssClass" style="@InputStyle"
		       type="search"
		       value="@_value"
		       @oninput="HandleInput"
		       placeholder="@Placeholder"
		       disabled="@Disabled"
		       id="@InputId"
		       @attributes="AdditionalAttributes"/>
		@if (Loading)
		{
			<span class="moka-search-spinner" aria-label="Loading">
				<MokaIcon Icon="MokaIcons.Status.Loading" SizeValue="14px" Class="moka-search-spinner-svg"/>
			</span>
		}
		else if (Clearable && !string.IsNullOrEmpty(_value))
		{
			<button type="button"
			        class="moka-search-clear"
			        @onclick="HandleClear"
			        tabindex="-1"
			        aria-label="Clear search">
				<MokaIcon Icon="MokaIcons.Navigation.Close" SizeValue="14px"/>
			</button>
		}
		else if (ShowShortcut && string.IsNullOrEmpty(_value))
		{
			<span class="moka-search-shortcut" aria-hidden="true">/</span>
		}
	</div>
</div>
