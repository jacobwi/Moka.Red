namespace Moka.Red.Core.Enums;

/// <summary>Specifies which directions a resizable element can be resized.</summary>
public enum MokaResizeDirection
{
	/// <summary>Resizable only horizontally (left/right edge).</summary>
	Horizontal,

	/// <summary>Resizable only vertically (top/bottom edge).</summary>
	Vertical,

	/// <summary>Resizable in both directions.</summary>
	Both
}
