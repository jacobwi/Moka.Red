# Moka.Red — LLM Session Log

Session-by-session change log for tracking what was done across Claude Code sessions.

---

## 2026-03-31 — Full Codebase Audit

### What was done
- Read every file in the repository (all 12 src packages, tests, samples, build config)
- Replaced `.claude/CLAUDE.md` with comprehensive, accurate documentation based on actual codebase state
- Created `.claude/AUDIT_PLAN.md` with 10 phased improvement tasks
- Created `LLM.txt` (this file) for session tracking

### Key findings
- **100+ components** across 12 packages, all functional
- **Shadow/elevation values are hardcoded** and duplicated in MokaPaper, MokaCard, MokaPanel (same rgba values copy-pasted)
- **CSS fallback hex values** are redundant since moka-tokens.css provides fallbacks
- **Inline SVGs** in MokaNotificationCenter violate the MokaIcon convention
- **Chevron/collapse CSS** is duplicated across Panel, Card, DockPanel
- **Shimmer animation** defined twice (Card and Skeleton)
- **No tests** for Primitives, Layout, Forms, Feedback, Data, Navigation, ContextMenu
- **Dead Bootstrap files** in DevApp wwwroot
- **Benchmarks project** has no benchmark classes

### Decisions
- Added rules to CLAUDE.md: confirm commit messages, no AI attribution, maintain LLM.txt
- Audit plan starts with token extraction (Phase 1) since shadows are the most duplicated problem
- Diagnostics package confirmed intentionally excluded from meta-package

### Files created/modified
- `.claude/CLAUDE.md` — complete rewrite
- `.claude/AUDIT_PLAN.md` — new file
- `LLM.txt` — new file (this file)

---

## 2026-03-31 — Phase 0: Simplified Consumer Onboarding

### What was done
- Merged `moka-reset.css` + `moka-tokens.css` + `moka-text.css` into single `moka.css` in Core wwwroot
- `MokaThemeProvider` now auto-injects `moka.css` via `<HeadContent>` — zero manual CSS links needed
- `MokaTabContainer` auto-injects `moka-tabs.css` via `<HeadContent>` when tabs are used
- Created unified `AddMokaRed()` in meta-package (`Moka.Red.Extensions`) that registers theme + all feedback services
- Renamed Core's extension to `AddMokaRedCore()` to avoid naming conflict
- Removed 12 redundant `<link>` tags from DevApp `App.razor` (scoped CSS already in `.styles.css`)
- Deleted dead Bootstrap files from `samples/Moka.Red.DevApp/wwwroot/lib/bootstrap/`
- Updated `README.md` quick start to reflect simplified onboarding
- Deleted old `moka-reset.css`, `moka-tokens.css`, `moka-text.css` files

### Consumer experience now
```csharp
// Program.cs — one line
builder.Services.AddMokaRed();
```
```razor
@* Layout — one component, CSS auto-injected *@
<MokaThemeProvider>@Body</MokaThemeProvider>
```

### Build & test results
- `dotnet build Moka.Red.slnx` — 0 warnings, 0 errors
- 124 tests pass (67 Core + 57 Diagnostics)

### Files changed
- `src/Moka.Red.Core/wwwroot/moka.css` — new (merged from 3 files)
- `src/Moka.Red.Core/wwwroot/moka-reset.css` — deleted
- `src/Moka.Red.Core/wwwroot/moka-tokens.css` — deleted
- `src/Moka.Red.Primitives/wwwroot/moka-text.css` — deleted
- `src/Moka.Red.Core/Theming/MokaThemeProvider.razor` — added HeadContent CSS injection
- `src/Moka.Red.Core/Extensions/ServiceCollectionExtensions.cs` — renamed to AddMokaRedCore()
- `src/Moka.Red/Extensions/ServiceCollectionExtensions.cs` — new unified AddMokaRed()
- `src/Moka.Red.Navigation/Tabs/MokaTabContainer.razor` — added HeadContent CSS injection
- `samples/Moka.Red.DevApp/Components/App.razor` — removed 12 CSS link tags
- `samples/Moka.Red.DevApp/Program.cs` — simplified to AddMokaRed()
- `samples/Moka.Red.DevApp/wwwroot/lib/bootstrap/` — deleted
- `README.md` — updated quick start
- `.claude/AUDIT_PLAN.md` — added Phase 0

---

## 2026-03-31 — Phase 1: Shadow & Elevation Tokens

### What was done
- Added 9 shadow tokens to `moka.css` and `MokaTheme.ToCssVariables()`:
  `--moka-shadow-0` through `--moka-shadow-4` (elevation scale),
  `--moka-shadow-popup`, `--moka-shadow-popup-lg`, `--moka-shadow-modal`, `--moka-shadow-subtle`
- Dark theme now generates stronger shadows (higher opacity) automatically
- Replaced hardcoded `box-shadow: rgba(...)` in 28 `.razor.css` files with token references
- 12 remaining rgba shadows are intentional micro-shadows (slider thumbs, switch knobs, color picker handles, diagnostics overlay) — left as-is

### Files changed (28 .razor.css files + 2 core files)
- `src/Moka.Red.Core/wwwroot/moka.css` — added shadow token definitions
- `src/Moka.Red.Core/Theming/MokaTheme.cs` — shadow tokens in ToCssVariables() with dark mode variants
- Layout: MokaPaper, MokaCard, MokaPanel, MokaToolbar, MokaAppBar, MokaFooter, MokaDockPanel
- Forms: MokaTimePicker, MokaDatePicker, MokaAutoComplete, MokaColorPicker, MokaSelect, MokaTagInput, MokaCreditCardInput
- Feedback: MokaToastHost, MokaDialog, MokaCommandPalette, MokaBottomSheet, MokaNotificationCenter, MokaPopover
- Primitives: MokaSegmentedControl, MokaScrollToTop, MokaCarousel, MokaCountdown, MokaMediaGallery
- Navigation: MokaTabStrip
- ContextMenu: MokaContextMenu
- ThemeGen: MokaThemePresets

### Build & test results
- `dotnet build Moka.Red.slnx` — 0 warnings, 0 errors
- 67 Core tests pass (including ToCssVariables tests)
- Browser verified: elevation shadows render identically to before

---

## 2026-03-31 — Phase 2: Remove Redundant CSS Fallback Values

### What was done
- Removed 211+ redundant `var(--moka-TOKEN, FALLBACK)` inline fallbacks from 39 `.razor.css` files
- Three pattern types cleaned: `#hex` fallbacks, `rgba()` fallbacks, and `var()` token-to-token fallbacks
- Preserved component-local custom properties (e.g., `--moka-swatch-columns`, `--moka-popover-offset-x`)
- All tokens are defined in `moka.css` `:root`, so removing inline fallbacks is safe

### Why this matters
- Single source of truth: changing a token value in `moka.css` or via `MokaTheme` now takes effect everywhere
- No more drift risk where inline fallbacks silently preserve stale values
- Cleaner CSS that's easier to read and maintain

### Build & test results
- `dotnet build Moka.Red.slnx` — 0 warnings, 0 errors
- 124 tests pass (67 Core + 57 Diagnostics)
- Browser verified: all components render correctly with colors, spacing, and typography intact

---

## 2026-03-31 — Phase 3: Deduplicate Shimmer Animation

### What was done
- Added shared `@keyframes moka-shimmer` to `moka.css`
- Removed 4 duplicate shimmer definitions from MokaCard, MokaTable, MokaSkeleton, MokaImage
- Updated animation references to use the shared `moka-shimmer` name

### Files changed
- `src/Moka.Red.Core/wwwroot/moka.css` — added shared keyframe
- `src/Moka.Red.Layout/Card/MokaCard.razor.css` — removed duplicate, updated reference
- `src/Moka.Red.Data/Table/MokaTable.razor.css` — removed duplicate, updated reference
- `src/Moka.Red.Feedback/Loading/MokaSkeleton.razor.css` — removed duplicate
- `src/Moka.Red.Primitives/Image/MokaImage.razor.css` — removed duplicate

---

## 2026-03-31 — Phase 4: Replace Inline SVGs with MokaIcon

### What was done
- Added `MokaIcons.Status.Bell` icon definition to `MokaIcons.cs`
- Replaced 3 inline `<svg>` elements in `MokaNotificationCenter.razor` with `<MokaIcon>`:
  - Bell button (18px) → `<MokaIcon Icon="MokaIcons.Status.Bell" SizeValue="18px"/>`
  - Empty state bell (32px) → `<MokaIcon Icon="MokaIcons.Status.Bell" SizeValue="32px" Style="opacity: 0.3"/>`
  - Dismiss X (12px) → `<MokaIcon Icon="MokaIcons.Navigation.Close" SizeValue="12px"/>`
- Zero inline SVGs remain in MokaNotificationCenter

### Files changed
- `src/Moka.Red.Icons/MokaIcons.cs` — added Bell icon
- `src/Moka.Red.Feedback/Notification/MokaNotificationCenter.razor` — replaced 3 SVGs with MokaIcon

---

## 2026-03-31 — Phase 5: Chevron/Collapse Pattern Extraction

### What was done
- Replaced CSS border-hack chevrons in Panel and DockPanel with `<MokaIcon>` using ChevronDown/Left/Right/Up
- Replaced inline SVG chevron in Card with `<MokaIcon>`
- Removed ~50 lines of duplicated CSS chevron ::before pseudo-element styles
- Added Icons + Primitives project references to Layout package (needed for MokaIcon)
- Added `ChevronIcon` property to MokaDockPanel.razor.cs for directional icon selection
- All collapse/expand animations work via CSS transform rotate on the MokaIcon class

### Dependency change
- `Moka.Red.Layout` now references `Moka.Red.Icons` + `Moka.Red.Primitives` (previously only Core)
- This is acceptable since most consumers install the meta-package

### Files changed
- `src/Moka.Red.Layout/Moka.Red.Layout.csproj` — added Icons + Primitives refs
- `src/Moka.Red.Layout/_Imports.razor` — added icon usings
- `src/Moka.Red.Layout/Panel/MokaPanel.razor` — MokaIcon chevron
- `src/Moka.Red.Layout/Panel/MokaPanel.razor.css` — replaced CSS chevron with icon rotation
- `src/Moka.Red.Layout/Card/MokaCard.razor` — MokaIcon chevron (was inline SVG)
- `src/Moka.Red.Layout/Card/MokaCard.razor.css` — updated to ::deep for icon targeting
- `src/Moka.Red.Layout/DockLayout/MokaDockPanel.razor` — MokaIcon directional chevron
- `src/Moka.Red.Layout/DockLayout/MokaDockPanel.razor.cs` — added ChevronIcon property
- `src/Moka.Red.Layout/DockLayout/MokaDockPanel.razor.css` — removed 43 lines of CSS chevron styles

### Build & test results
- `dotnet build Moka.Red.slnx` — 0 warnings, 0 errors
- 124 tests pass
- Browser verified: Panel collapse toggle, DockPanel directional chevrons all working

---

## 2026-03-31 — Phase 6: Fixed Size Tokenization + Missing Token Fix

### What was done
- Added `--moka-height-statusbar`, `--moka-height-toolbar`, `--moka-height-toolbar-dense` tokens to moka.css
- Added `--moka-z-appbar` to z-index scale (was used but never defined — components relied on removed fallbacks)
- Added `--moka-color-on-surface-variant` to palette system:
  - New property `OnSurfaceVariant` on `MokaPalette` record (light: #666666, dark: #999999)
  - Added to `MokaTheme.ToCssVariables()` output
  - Added to `moka.css` fallback tokens
- Replaced hardcoded pixel heights with tokens in StatusBar and Toolbar CSS

### Critical bug found and fixed
- Phase 2 removed inline CSS fallbacks, but `--moka-color-on-surface-variant` and `--moka-z-appbar` were never defined in either `moka.css` or `ToCssVariables()` — they only worked via the now-removed inline fallbacks. Fixed by adding proper token definitions.

### Files changed
- `src/Moka.Red.Core/wwwroot/moka.css` — added on-surface-variant, z-appbar, height tokens
- `src/Moka.Red.Core/Theming/MokaPalette.cs` — added OnSurfaceVariant property
- `src/Moka.Red.Core/Theming/MokaTheme.cs` — added on-surface-variant to ToCssVariables
- `src/Moka.Red.Layout/StatusBar/MokaStatusBar.razor.css` — height token
- `src/Moka.Red.Layout/StatusBar/MokaStatusBarItem.razor.css` — line-height token
- `src/Moka.Red.Layout/Toolbar/MokaToolbar.razor.css` — height tokens

### Phase 7 (Bootstrap cleanup) — already completed in Phase 0

---

## 2026-03-31 — Phase 8: Test Coverage Expansion

### What was done
- Created 3 new test projects: Primitives.Tests, Layout.Tests, Forms.Tests
- Added 64 new tests across 7 test classes:
  - **MokaButtonTests** (12): root class, variant/color/size classes, disabled, loading spinner, click events, fullwidth, icon-only, user class
  - **MokaIconTests** (7): SVG rendering, path data, size/color classes, custom SizeValue, user class
  - **MokaBadgeTests** (10): content display, count truncation, dot mode, standalone, visibility, color, position
  - **MokaFlexboxTests** (11): direction, justify, align, wrap, gap, inline, breakpoints, user class
  - **MokaCardTests** (8): elevation classes, outlined, title/subtitle, clickable, fullwidth
  - **MokaCheckboxTests** (8): input rendering, label, disabled, toggle, indeterminate, user class
  - **MokaSwitchTests** (6): label, disabled, label placement, toggle, user class
- Added all 3 projects to Moka.Red.slnx

### Test totals
- Before: 124 tests (2 projects)
- After: 188 tests (5 projects) — **+64 tests, +3 projects**

---

## 2026-03-31 — Phase 9: Add Benchmark Cases

### What was done
- Created 3 benchmark classes with 16 total benchmarks:
  - **CssBuilderBenchmarks** (6): single class, 3 classes, 6 with conditions, 10 realistic, null skips, func conditions
  - **StyleBuilderBenchmarks** (6): empty, single, 3 properties, flexbox-realistic, null skips, conditional
  - **ThemeBenchmarks** (4): light/dark/custom ToCssVariables, create+generate
- Added CA1822 suppression (BDN requires instance methods)
- All 16 benchmarks discovered via `--list flat`

---

## 2026-03-31 — Phase 10: Documentation & Final Review

### What was done
- Updated `.claude/CLAUDE.md` to reflect all session changes:
  - Repository structure updated (new test projects, renamed CSS files, Layout deps)
  - Known Issues section updated (7 of 10 resolved)
  - Hardcoded Values section replaced with "Remaining" (only intentional micro-shadows left)
  - Duplication section reduced to 1 remaining item (scrollbar styling)
  - Component Registry: MokaNotificationCenter upgraded to ✅ Stable
  - Inter-package dependencies updated for Layout → Icons + Primitives
  - Inconsistencies section updated (inline SVGs mostly resolved)
- Updated `.claude/AUDIT_PLAN.md` phases status

### Session totals
| Phase | What | Files Changed |
|-------|------|--------------|
| 0 | Consumer onboarding (CSS + services) | 12 |
| 1 | Shadow/elevation tokens | 30 |
| 2 | Removed redundant CSS fallbacks | 39 |
| 3 | Deduplicated shimmer animation | 5 |
| 4 | Replaced inline SVGs with MokaIcon | 2 |
| 5 | Chevron/collapse extraction | 9 |
| 6 | Size tokenization + missing token fix | 6 |
| 7 | Bootstrap cleanup (done in Phase 0) | — |
| 8 | Test coverage expansion (+64 tests) | 10 new files |
| 9 | Benchmark cases (16 benchmarks) | 3 new files |
| 10 | Documentation & final review | 2 |

**Build:** 0 warnings, 0 errors
**Tests:** 188 pass (67 Core + 57 Diagnostics + 30 Primitives + 20 Layout + 14 Forms)
**Benchmarks:** 16 across 3 classes

---

## 2026-04-04 — Component Expansion + Full Audit

### New components added (28 total this session)
Drawer, Steps, Tag, Calendar, KanbanBoard, Meter, InputGroup, KeyValue,
TreeSelect, TransferList, ColorWheel, DataList, FloatingActionButton,
PinInput, Marquee, NumberTicker, Gauge, Confetti, CodeBlock, SwipeActions,
PasswordStrength, InfiniteScroll, ColorInput, SplitButton, DateRangePicker,
Notice, ToggleGroup, HoverCard

### Full audit fixes (10 issues)
1. MokaThemeProvider: removed IsFixed from CascadingValue
2. MokaSegmentedControl: removed IsFixed, fixed size CSS selectors
3. MokaTooltip: added missing CssClass override
4. MokaDialog: renamed DialogCss to CssClass override, added ShouldRender
5. MokaAccordionItem: renamed ItemCssClass to CssClass override
6. MokaRadioItem: renamed ItemCssClass to CssClass override
7. MokaTreeSelect: changed to MokaVisualComponentBase, added ShouldRender
8. MokaProgress: changed to MokaVisualComponentBase, renamed Rounded to RoundedEnds
9. MokaKanbanBoard: added missing ShouldRender
10. MokaAlert: added missing ShouldRender

### Other fixes
- MokaTag: removed `new` keyword on Parameter properties, use `override` instead
- ColorWheel: added click handler on SL area
- SegmentedControl size selectors targeting wrong CSS classes
- CSS token sweep: 50+ files, 238 replacements
- Scrollbar utilities extracted to moka.css
- DevApp PrimitivesPage: fixed broken @code blocks, extracted to code-behind

### Documentation
- 42 doc pages total (14 existing + 28 new)
- Imports guide with recommended _Imports.razor
- DevApp sidebar: All/Categories segmented toggle with 115+ component links

### Knowledge base updated
- CLAUDE.md: added 6 new non-negotiable rules (no `new` on params, no IsFixed, ShouldRender, CssClass override, no duplicates, single @code block)
- Memory: component inventory (167), no-duplicates rule, conventions from bugs

### Final state
- **167 components** across 10 packages
- **222 tests** across 7 projects
- **16 benchmarks** across 3 classes
- **42 doc pages**
- **0 warnings, 0 errors**

---

## 2026-04-05 — MokaFormBuilder + MokaCommandBar Docs & Demos

### What was done
- Fixed MokaFormBuilder component (.razor, .razor.cs, .razor.css) to compile cleanly
- Fixed MokaFormBuilderExport.cs CA1305/CA1307 analyzer errors (CultureInfo.InvariantCulture, StringComparison.Ordinal)
- Created documentation pages:
  - `docs/docs/components/command-bar.md` (order: 63) — 4 examples: basic, breadcrumb+actions, dense, elevated
  - `docs/docs/components/form-builder.md` (order: 64) — 3 examples: basic, pre-populated, export to Razor
- Updated `docs/mokadocs.yaml` — added both pages to Components nav + usings for CommandBar/FormBuilder
- Updated `docs/docs/getting-started/imports.md` — added @using for CommandBar + FormBuilder namespaces
- Appended CommandBar demo to NavigationPage.razor (breadcrumb left + action buttons right, dense+elevated variant)
- Appended FormBuilder demo to FormsPage.razor (3 pre-populated fields)
- Updated DevApp `_Imports.razor` with CommandBar + FormBuilder namespaces
- Updated NavMenu `_allComponents` with CommandBar + FormBuilder entries

### Files created
- `docs/docs/components/command-bar.md`
- `docs/docs/components/form-builder.md`

### Files modified
- `src/Moka.Red.Forms/FormBuilder/MokaFormBuilder.razor` — restored to match .cs file
- `src/Moka.Red.Forms/FormBuilder/MokaFormBuilder.razor.css` — complete CSS for 3-panel layout
- `src/Moka.Red.Forms/FormBuilder/MokaFormBuilderExport.cs` — fixed CA1305/CA1307 errors
- `src/Moka.Red.Forms/_Imports.razor` — added FormBuilder namespace
- `docs/mokadocs.yaml` — added nav items + usings
- `docs/docs/getting-started/imports.md` — added namespaces + table updates
- `samples/Moka.Red.DevApp/Components/Pages/NavigationPage.razor` — CommandBar demo
- `samples/Moka.Red.DevApp/Components/Pages/FormsPage.razor` — FormBuilder demo
- `samples/Moka.Red.DevApp/Components/_Imports.razor` — added namespaces
- `samples/Moka.Red.DevApp/Components/Layout/NavMenu.razor` — added entries

### Build result
- `dotnet build Moka.Red.slnx` — 0 warnings, 0 errors

---

## 2026-04-05 — Diagnostics Package Enhancements (3 New Panels)

### What was done
- Added 3 new panels to the Diagnostics overlay (now 10 panels total):
  1. **NetworkPanel** — JS interop call stats with sortable columns (method, calls, total ms, avg ms, max ms), color-coded rows for slow/hot calls, summary footer
  2. **ComponentTreePanel** — Component hierarchy grouped by type with expand/collapse per type, heat-coded dots (green <=5 renders, yellow 6-20, red >20), disposed badge, instance details
  3. **MemoryPanel** — GC gen 0/1/2 collection counts with bar chart visualization, total/managed memory cards, GC info (latency mode, server GC, finalization queue), Force GC button, auto-refresh every 2 seconds
- Updated `MokaDiagnosticsOverlay.razor` to include Tree, Network, and Memory tabs

### Conventions followed
- All panels use the existing dark overlay hex color scheme (Diagnostics-specific, not token-based per CLAUDE.md)
- MemoryPanel overrides `ShouldRender() => true` (auto-refresh via timer)
- All panels implement `IDisposable` with timer cleanup
- XML doc comments on all public types
- `.razor` + `.razor.cs` + `.razor.css` triad for each panel
- BEM-style CSS class naming: `moka-diag-{panel}-{element}--{modifier}`

### Files created (9)
- `src/Moka.Red.Diagnostics/Components/Panels/NetworkPanel.razor`
- `src/Moka.Red.Diagnostics/Components/Panels/NetworkPanel.razor.cs`
- `src/Moka.Red.Diagnostics/Components/Panels/NetworkPanel.razor.css`
- `src/Moka.Red.Diagnostics/Components/Panels/ComponentTreePanel.razor`
- `src/Moka.Red.Diagnostics/Components/Panels/ComponentTreePanel.razor.cs`
- `src/Moka.Red.Diagnostics/Components/Panels/ComponentTreePanel.razor.css`
- `src/Moka.Red.Diagnostics/Components/Panels/MemoryPanel.razor`
- `src/Moka.Red.Diagnostics/Components/Panels/MemoryPanel.razor.cs`
- `src/Moka.Red.Diagnostics/Components/Panels/MemoryPanel.razor.css`

### Files modified (1)
- `src/Moka.Red.Diagnostics/Components/MokaDiagnosticsOverlay.razor` — added 3 new tab buttons + panel switch cases

### Build result
- `dotnet build Moka.Red.slnx` — 0 warnings, 0 errors

---

## 2026-04-08 — Docs preview-host overhaul + 0.1.6 FrameworkReference fix

### What was done
- **Bumped to 0.1.6** in `Directory.Build.targets`. The 0.1.5 release intended to fix
  NETSDK1082 for WASM consumers but only added the `Microsoft.AspNetCore.Components.Web`
  PackageReference substitution — the implicit `Microsoft.AspNetCore.App` `FrameworkReference`
  was still being added by the SDK and propagated into the published nuspec.
- Added a new MSBuild `<Target Name="StripAspNetCoreAppFrameworkRef"
  BeforeTargets="_CheckForUnsupportedNETCoreVersion;ProcessFrameworkReferences;ResolveFrameworkReferences">`
  in `Directory.Build.targets` that runs for `IsPackable=true` projects and removes
  `Microsoft.AspNetCore.App` from the FrameworkReference items collection BEFORE the SDK's
  unsupported-version check fires.
- Verified by extracting `Moka.Red.0.1.6.nupkg`'s nuspec — `<frameworkReferences>` group is
  no longer present (vs. 0.1.3 which had it for both net9.0 and net10.0 targets).
- Deleted `nuget.config` — the file only had `<clear /> + nuget.org` which is the global
  default, so the file was redundant. Removed to avoid confusing future contributors.

### docs/ overhaul
- `docs/mokadocs.yaml` — replaced the verbose `previewHost: …`, `references: […]`,
  `stylesheets: […]` plugin block with a single `library: Moka.Red@0.1.6` line. The
  `mokadocs-blazor-preview` plugin (v3.x in `mokadocs` repo) now auto-discovers a
  `./preview-host/` Blazor WebAssembly project, scaffolds it from a generic library-
  agnostic template if missing, and runs `dotnet publish` on it transparently. The yaml
  no longer points across the repo boundary at the sibling `Moka.BlazorRepl` repo.
- The auto-scaffolded `docs/preview-host/` project (csproj + Program.cs + wwwroot/index.html
  + empty Directory.* shadow files) is owned by the user after first scaffold; mokadocs
  never overwrites it. Edit `Program.cs` to register library-specific services and
  `wwwroot/index.html` to add CSS/font links between the customization markers.

### Files modified
- `Directory.Build.targets` — bumped Version to 0.1.6, added `StripAspNetCoreAppFrameworkRef` Target
- `CHANGELOG.md` — added [0.1.6] entry
- `docs/mokadocs.yaml` — replaced previewHost/references/stylesheets with `library:` field
- `nuget.config` — deleted

### Build result
- `dotnet pack Moka.Red.slnx -c Release -o nupkgs` — all 12 packages packed at 0.1.6, 0 errors
- `Moka.Red.0.1.6.nupkg` nuspec verified to have NO `<frameworkReferences>` group

---

## 2026-07-05 - CLAUDE.md refresh + Moka.Note style port (dims, boot screen, runtime theming)

### CLAUDE.md full refresh
- Rebuilt .claude/CLAUDE.md from a 24-agent scan: 211-component registry (now 212) with verified
  parameters, corrected version (0.1.8), service registration table, structure tree, test counts.
- Confirmed dead code: MokaContainerBase (Layout/Base) and MokaDataComponentBase<T> (Data/Base)
  have zero inheritors. Seven .gitkeep-only placeholder folders documented.

### Task 1: Semantic dim tokens (from Moka.Note)
- MokaPalette: ErrorDim, WarningDim, SuccessDim, InfoDim + PrimaryGlowFaint (light + dark values)
- Emitted as --moka-color-*-dim and --moka-color-primary-glow-faint in ToCssVariables() and
  moka.css fallbacks (light :root + .moka-dark)

### Task 2: MokaBootScreen (component #212, Feedback package)
- New component BootScreen/MokaBootScreen (.razor/.razor.cs/.razor.css): terminal-style splash -
  brand mark with glow, pulsing status dot, spec chips, indeterminate bar, faint accent grid,
  scanline. Params: Visible, Brand, BrandSecondary, StatusText, ChipsContent, ShowGrid,
  ShowScanline, ShowBar, FullScreen. Token-driven, inherits MokaVisualComponentBase.
- New static twin src/Moka.Red.Core/wwwroot/moka-boot.css: self-contained pre-boot splash for
  host pages (cannot use theme tokens - runs before Blazor). Ported from Moka.Note WASM host.
- Wired into mokared-wasm and mokared-maui template index.html (link + .moka-boot markup).
- DevApp: FeedbackPage demo section + NavMenu entry + _Imports namespace.

### Task 3: Runtime accent/density theming (pattern from Moka.Note ThemeHost)
- MokaTheme.WithAccent("#hex"): recomputes Primary (+Light/Dark via channel mix), all glow/border
  tokens, dark outlines; dark shadow rings now derive from Palette.Primary in ToCssVariables()
  (fall back to interned red constants when primary is not parseable hex).
- MokaTheme.WithDensity(x): scales spacing tokens (0.75 compact / 1.0 cozy / 1.15 comfy);
  MokaTheme.WithFontScale(x): scales font-size tokens. Both emitted as --moka-density /
  --moka-font-scale for consumer calc() use.
- Helpers: TryParseHex (#rgb/#rrggbb), Rgba, Mix, ScaleSize (invariant culture).

### Verification
- dotnet build Moka.Red.slnx: 0 errors (CS1668 warnings are a stale LIB env var, unrelated)
- Tests: Core 76/76 (+10 new theme tests), Feedback 30/30
- Browser-verified BootScreen demo in DevApp: tokens resolve, grid/scanline/pulse animate

### Not done (follow-ups)
- docs/ site page for BootScreen (mokadocs components page + nav entry)
- Moka.Note component candidates (features/layout scan reports saved in session scratchpad)

---

## 2026-07-09 - 5 Moka.Note components, context-menu service, sticky pagination, table fix, tests

### 5 new components ported from Moka.Note (charts excluded per user)
- MokaSelectionBar (Primitives) - floating bulk-action bar, renders only when Count > 0
- MokaStatusDot (Primitives) - signature glow-dot status indicator (pulse/glow/color/size)
- MokaDiffViewer (Primitives) - mono LCS line-diff (LineDiffEngine internal, MokaDiffLine record)
- MokaCheatsheet (Feedback) - keyboard-shortcut overlay (MokaCheatsheetGroup/Item, MokaKbd chips)
- MokaSlashMenu (Feedback) - textarea-anchored "/" palette with public HandleKeyAsync delegation contract
- Wired _Imports (Primitives/Feedback/DevApp), DevApp demos (Primitives/FeedbackPage) + NavMenu entries.
- LineDiffEngine: jagged int[][] LCS (CA1814 fix), normalizes line endings, naive fallback > 2000 lines/side.

### MokaTable responsiveness fix
- .moka-table-wrapper + .moka-table-container got min-width: 0 (flexbox min-width:auto trap caused
  horizontal page overflow instead of the scroll container scrolling internally). Browser-verified at 375px.

### Context-menu service (new) + item right-click hooks
- IMokaContextMenuService / MokaContextMenuService (scoped) + MokaContextMenuHost, mirrors Toast/Dialog.
  Show(x,y,items) / Show(MouseEventArgs, items) / Close() + OnChanged. AddMokaContextMenu() wired into
  AddMokaRed(). Consumer places one <MokaContextMenuHost/> (added to DevApp MainLayout).
- Item components surface right-clicks (Layer A, decoupled - respects the ContextMenu->Primitives
  dependency, so no circular ref): MokaTable.OnRowContextMenu, MokaKanbanBoard.OnCardContextMenu,
  MokaSortable.OnItemContextMenu carry Core/Interactions/MokaItemContextMenuArgs<T>; MokaListItem,
  MokaTreeItem, MokaMenuItem.OnContextMenu carry MouseEventArgs. Each wires @oncontextmenu:preventDefault
  only when a handler is attached. Skipped: MokaDataList (display-only), MokaTransferList (has move buttons).
- Layer B (first-class RowContextMenuItems Func) deferred - needs MokaContextMenuItem + interface promoted
  to Core (breaking); tracked as a task, needs user sign-off.
- Browser-verified: right-click table row opens the shared menu at cursor; item click fires action + closes.

### Sticky pagination
- MokaPagination.Sticky + StickyPosition (MokaPaginationStickyPosition Top/Bottom): position: sticky with
  surface bg + border. MokaTable.StickyPagination pins the footer + unclips the wrapper (overflow:hidden
  traps position:sticky). Verified position:sticky; bottom:0.

### Unit tests (+55, all passing)
- Primitives.Tests 30 -> 58: MokaStatusDotTests (10), MokaSelectionBarTests (9), MokaDiffViewerTests (9)
- Feedback.Tests 30 -> 57: MokaBootScreenTests (10), MokaCheatsheetTests (6), MokaSlashMenuTests (11)
- Solution total now 272 tests across 7 projects.
- Batch B still untested (no test project for those packages): context-menu service, pagination sticky,
  the item-hook EventCallbacks - tracked as a follow-up task.

### Counts / docs
- 218 components (Primitives 79, Feedback 26, ContextMenu 3). CLAUDE.md registry + memory updated.
- CLAUDE.md rule 22 added: adding/changing a component requires a docs-site update (may batch to end).

### Not done (tracked as tasks)
- docs/ site pages for the new components (mokadocs), Batch B tests (new ContextMenu.Tests + Data.Tests),
  Layer B context-menu promotion to Core, commit (needs message approval).

---

## 2026-09-12 - Internal Docs + NuGet Upgrade

### What was done
Two tracks: a deep codebase study written up as internal docs under `.claude/docs/`, and a full
dependency upgrade including the xunit v3 -> v4 / Microsoft.Testing.Platform migration.

### Internal docs (new, `.claude/docs/`)
Nine files plus an index, written from a fresh full-source audit (5 parallel explorer agents covering
Core+Icons, Forms+Feedback, Data+Navigation+ContextMenu, Primitives+Layout, and tests/samples/docs/CI):

- `README.md` - index, 60-second orientation, health snapshot
- `01-architecture.md` - package graph, layering constraints, multi-target + NETSDK1082 story, CSS delivery
- `02-base-classes.md` - the two hierarchies, the render latch, SafeJsInvokeAsync semantics, disposal
- `03-theming.md` - the 101-token contract, fluent API, density/font scale, dark-mode gaps
- `04-component-authoring.md` - end-to-end checklist for adding a component
- `05-services-and-hosts.md` - DI table, all five services with full signatures, the ContextMenu layering constraint
- `06-js-interop.md` - every JS module with exported signatures, the eval sites
- `07-build-test-release.md` - toolchain, commands, CI, the six version strings a release must move
- `08-gotchas.md` - the defect catalogue with file:line references
- `09-data-table.md` - MokaTable deep-dive including its ~13 unreachable parameters

CLAUDE.md now points at `.claude/docs/README.md` and got 10 new Known Issues entries plus corrected
dependency and test-count tables.

### NuGet upgrade (all verified)
| Package | From | To |
|---|---|---|
| xunit.v3 | 3.2.2 | 4.0.0 |
| xunit.runner.visualstudio | 3.1.5 | 4.0.0 |
| Microsoft.NET.Test.Sdk | 18.3.0 | 18.10.0 |
| bunit | 2.7.2 | 2.10.3 |
| Microsoft.Testing.Extensions.TrxReport | - | 2.3.3 (new) |
| Microsoft.AspNetCore.Components.WebAssembly(.DevServer) | 10.0.9 | 10.0.12 |
| Microsoft.AspNetCore.Components.Web (net9.0) | 9.0.7 | 9.0.20 |
| Microsoft.AspNetCore.Components.Web (net10.0) | 10.0.5 | 10.0.12 |
| Moka.Blazor.Repl.Host (docs preview host) | 1.3.4 | 1.3.5 |
| WebAssembly / DevServer / WebAssembly.Server (4 templates) | 10.0.5 | 10.0.12 |

BenchmarkDotNet 0.15.8 was already latest.

### Test-platform migration (the only breaking part)
xunit.v3 4.x runs on Microsoft.Testing.Platform, and the .NET 10 SDK dropped the VSTest bridge for MTP.
Every `dotnet test` failed with "Testing with VSTest target is no longer supported by
Microsoft.Testing.Platform on .NET 10 SDK and later."

Fix: a `test.runner` block in `global.json` (NOT `dotnet.config` - that has no effect, and neither does
`-p:TestingPlatformDotnetTestSupport=true`):

```json
"test": { "runner": "Microsoft.Testing.Platform" }
```

Knock-on changes:
- VSTest's `--logger "trx;LogFileName=..."` is gone. MTP uses `--report-trx --report-trx-filename <name>`,
  which needs the `Microsoft.Testing.Extensions.TrxReport` package. Added centrally + referenced from all
  7 test projects. Output lands in `./TestResults/` (already gitignored).
- `.github/workflows/ci.yml` test step updated to the MTP flags.
- Positional project/solution paths still work, so the documented `dotnet test tests/X` commands are fine.

### Verification
- `dotnet build Moka.Red.slnx` - 0 warnings, 0 errors
- `dotnet build Moka.Red.slnx -c Release -warnaserror` - 0 warnings, 0 errors
- `dotnet test Moka.Red.slnx --no-build` - 286/286 passing (up from 272 methods; 271 methods + 19 InlineData rows)
- `dotnet pack Moka.Red.slnx -c Release` - all 12 packages
- NETSDK1082 guard confirmed intact: Core nuspec has no `frameworkReference`, only
  `Microsoft.AspNetCore.Components.Web` 9.0.20 / 10.0.12 per TFM

### Memories written (`.claude/projects/.../memory/`)
internal-docs location, git safe.directory quirk for this path, sibling repos (Moka.Note /
Moka.BlazorRepl / Moka.Blazor.AI), the uncommitted worktree state, external links.

### Findings
Catalogued in `.claude/docs/08-gotchas.md`, then all fixed later the same day (see the next entry).

---

## 2026-09-12 - Full Defect Sweep

Acted on every finding from the audit earlier the same day. Six parallel agents partitioned by
package (strict file ownership, no concurrent builds) plus Core, Icons, Forms, templates and
repo hygiene done directly.

### Verification
- `dotnet build Moka.Red.slnx -c Release -warnaserror` - 0 warnings, 0 errors
- `dotnet test Moka.Red.slnx` - **371/371 passing** (was 286)
- `dotnet pack` - all 12 packages; NETSDK1082 guard confirmed intact

### The bugs that actually mattered

**MokaBarcode was lying about four of its five formats.** `MokaBarcodeFormat` advertises
Code128/Code39/EAN13/EAN8/UPC but every value routed to `GenerateCode128`. Code 39, EAN-13,
EAN-8 and UPC-A are now real encoders with check-digit computation and validation, L/G/R parity
tables, proper guard patterns and per-format quiet zones (the old renderer drew flush to the
canvas edge, which makes any symbol unscannable). 14 new tests assert the spec module counts
(95/67/95), guard offsets, and that UPC-A equals EAN-13 with a leading zero.

**MokaMarquee never scrolled at all.** `animation-duration: calc(100vw / var(--speed) * 1s)` is
invalid CSS - calc cannot divide a length by a length - so the declaration was dropped entirely.
Duration is now computed in C# and `IsVertical` (previously computed and unused) selects the
loop distance.

**EditForm validation was invisible library-wide.** `MokaInputBase.ComponentCssClass` had zero
consumers, so the framework's `modified`/`valid`/`invalid` classes never reached the DOM, and
`HasError` was purely `!string.IsNullOrEmpty(ErrorText)` in every component. Added
`ValidationMessages`/`HasValidationError`/`ValidationErrorText` to the base and wired 11
components through it. 8 new tests render a real `EditForm` + `DataAnnotationsValidator` and
assert the message appears.

**MokaTable had ~13 parameters with no UI behind them.** Filter row, inline editing, keyboard
navigation, row reordering and column resizing were all declared and unreachable; `RowReorderable`
even rendered a header cell with no matching body cell, so every data row was one short. All
wired. Also fixed: `<Virtualize>` in `<tbody>` without `SpacerElement="tr"`, an unchecked
`ICollection<T>` cast, boxing that made expand rows never open for struct items, and
`SelectedItems` being aliased rather than copied.

**Only one dialog could ever be shown.** A second `ConfirmAsync`/`ShowAsync` overwrote the pending
`TaskCompletionSource` and the first `await` never completed. Requests now queue, `Close(false)`
resolves to `false` rather than `null`, and `Dispose` resolves everything pending. 10 new tests.

**CSS isolation was silently breaking selectors.** Converting inline `<svg>` to `<MokaIcon>`
moves the element into MokaIcon's scope, so `.moka-chip__close svg` stops matching - the rewriter
appends the scope to the last compound selector. Six components needed `::deep`, and that also
repaired pre-existing dead rules on usages that were already MokaIcon.

**Duplicate bare selectors.** `MokaCallout` had five `.moka-callout__icon` rules (one per type);
only the last applied, so the per-type icon colours never rendered. `MokaStat` had three size
variants with the same problem, where a hardcoded `32px` won.

### Everything else fixed
QR mask penalty rule 3; Identicon entropy past 31 bits; MokaScrollToTop and MokaCommandPalette
JS listener leaks; `lockBodyScroll` reference counting; `trapFocus` actually wired; MokaSplitPane
and MokaInfiniteScroll `eval` injection; MokaResizable's inert corner handle; MokaTransferList and
MokaCalendar render latches; context-menu focus, viewport clamping, submenu measurement and
divider-shifted highlight; tab pin/unpin ordering, bulk closes, `TabAdded` and lossy restore;
command palette thread safety and `Href`; notification `Icon`/`OnClick` reachability; toast
backlog; `.moka-dark`'s 7 missing tokens; inverted scrollbar hover; `prefers-reduced-motion`;
acronym kebab-case (`EAN13` was becoming `e-a-n13`); string-ordered breakpoints; culture-dependent
CSS lengths in five places; ~50 raw SVGs; 17 hardcoded colours; UTF-8 CSV export.

### Deleted (breaking)
`MokaPopupBase` + `PopupPosition`, `MokaDataComponentBase<T>`, `MokaContainerBase` - all public
API with zero inheritors. `MokaSortable.Group` and the `_mokaSortableGroups` registry, which
advertised cross-list drag that was never implemented.

### Added
`Moka.Red.Data.Tests` (13 cases, the package had none). `InternalsVisibleTo` for
`Moka.Red.Primitives.Tests` so the internal barcode and QR encoders are testable. 12 icons.
`MokaIconDefinition.Filled`. The four `dotnet new` template options that did nothing
(`noHttps`, `allInteractive`, `pwa`, `supportBrowser`) now work, with real service-worker and
manifest files for the PWA path.

### Repo hygiene
README (60+ -> 218 components, ThemeGen added), version caption, 14 orphan docs pages given nav
entries, package descriptions advertising components that do not exist, 7 empty placeholder
folders, Bootstrap out of WasmApp, 25 dead permission entries in settings.local.json, em dashes
swept out of ~40 files.

### Docs updated
`.claude/CLAUDE.md` (rule 5 exception list, registry, structure, dependencies, test counts,
Known Issues rewritten), `.claude/docs/{02,06,08}`, `CHANGELOG.md` Unreleased section.

### Raw SVG cleanup, final numbers
Started at 50+ inline `<svg>` elements across the library, now **exactly 7**, all on the rule 5
exception list: MokaIcon (the renderer), MokaGauge and MokaKnob (computed arcs), MokaProgress
(circular ring), MokaCheckbox (checkmark + indeterminate dash), MokaButton and MokaSpinner
(`stroke-dasharray` circles driven by CSS animations). Barcode/QRCode/Identicon emit their SVG
as `MarkupString` and are exempt by nature.

Re-anchoring the CSS was the real work. 5 rules broke because of this pass and 11 more were
**already dead** on usages that were converted to `MokaIcon` in an earlier release, including
`MokaToggleGroupItem`, `MokaTreeSelect` (4 rules), `MokaDropdownItem`, `MokaCommandPalette`
(2 rules) and `MokaContextMenu`. Those icons had simply never been styled.

`MokaRating` stopped hand-rendering `SvgPath`/`ViewBox` off its `Icon`/`FilledIcon` parameters
and now delegates to `MokaIcon`, defaulting to `MokaIcons.Toggle.Star` / `Toggle.StarOutline`.
Its `.moka-rating-star svg` sizing rule is gone (replaced by `SizeValue="1em"`), and the filled
star renders via the new `Filled` flag rather than fill-plus-stroke, so it is a hair smaller at
the edge.

### Not done
- No commit (rule 19, needs message approval).
- Docs-site pages for the newly wired MokaTable features, the new Feedback service members and
  the tab session-state additions (rule 22).
- No version bump; the Unreleased section is ready for whatever number you pick.

---

## 2026-09-12 - Dev launcher (scripts/RunSamples.bat) and mokadocs 1.6.0

### scripts/RunSamples.bat (new)
One menu for running anything locally, named and shaped after Moka.Docs\scripts\RunSamples.bat so
both repos take the same arguments: interactive, or `RunSamples.bat <choice> [option]`.

Choices: devapp, wasm, docs, build, test, bench, pack, clean, utils (restore, servers, port, certs,
info). Flags: --no-browser, --dry-run, --no-color. Env: MOKA_PACK_OUT, MOKA_DOCS_PORT, MOKA_DOCS_LOCAL.

What makes it more than a menu:
- Reads each sample's URL from launchSettings.json instead of hardcoding ports.
- Detects a busy port, names the owning process, offers to open the existing instance or stop it.
- Opens the browser only once the app responds, via a minimized relaunch of itself using
  `curl --retry-connrefused --retry-max-time`.
- Never sets DOTNET_WATCH_SUPPRESS_LAUNCH_BROWSER just to avoid a duplicate tab, because that variable
  also disables hot-reload page refresh. WasmApp's profile has launchBrowser true so dotnet watch
  opens it; DevApp's does not, so the script's waiter does.
- Lists test projects dynamically, runs one by name fragment, or filters by test name across the
  solution with --ignore-exit-code 8 so projects with no matches do not fail the run.
- Pack verifies the WebAssembly guard afterwards (no frameworkReference in the Core nuspec).
- mokadocs: detects the installed global tool, compares against the latest on NuGet, offers
  install/update, and can run from the sibling Moka.Docs source instead.

### Bugs found by testing it (all fixed before shipping)
- `find` and `ping` resolved to Git for Windows' Unix tools when they come first on PATH. Unix find
  treats "," as a directory; Unix `ping -n` means numeric output and pings forever, which would have
  hung the browser waiter. Both pinned to System32, along with timeout.exe.
- `pause` hitting end of input on a pipe leaves the next `choice` blocked forever. Replaced with
  `timeout /t -1`, which waits for any key at a console but returns immediately when input is
  redirected.
- The waiter counted loop iterations, but probing a closed localhost port costs ~2s on Windows, so
  "240" meant ~13 minutes. Now time-capped through curl's retry options.
- `mokadocs validate` is a stub ("not yet implemented"), so the menu no longer offers it.

Verified: help; dry runs of every target; real `test data` (13 pass) and `test filter:Barcode` (21 pass)
with exit 0; a genuinely failing pack propagating exit 1; a real pack into the scratchpad with the
guard passing; the guard failing on a crafted bad nupkg; the port inspector naming svchost.exe on 135;
the busy-port prompt; the waiter's give-up timing; curl's retry succeeding a second after a delayed
server came up; and the interactive menu driven with piped keys across every submenu.

### mokadocs 1.6.0
- CLI flags Moka.Red uses (`serve --port --no-open`, `build --base-path`) are unchanged.
- `theme.name: default` is reserved for the embedded theme, so the new resolver does not warn.
- A real 1.6.0 build of Moka.Red's docs succeeded: 93 pages, 860 search entries, the Blazor preview
  host published into `_site/_preview-wasm`, 376 snippet assemblies compiled, `.nojekyll` written.
- Added `.mokadocs/` to .gitignore. 1.6.0 caches Roslyn analysis there, though only for
  `content.projects`, which Moka.Red does not configure, so nothing is written today.
- `mokadocs doctor`: 8 passed, 3 warnings, 0 errors, no broken internal links. Its "unknown plugins"
  warning for mokadocs-blazor-preview and mokadocs-changelog is a false positive in Moka.Docs: the
  known-plugin list uses short names while the plugins declare the prefixed IDs.

### Docs previews: fixed
The first pass here counted 33 failing previews on 17 pages. The real number was 24 compile
failures on 15 pages. The other 9 (toast, getting-started/quick-start) are the plugin's noscript SSR
fallback, which renders with a bare container that has no IMokaToastService. The live iframe
previews on those pages work because preview-host/Program.cs calls AddMokaRed(). That fallback is a
Moka.Docs plugin limitation and was left alone.

The preview host compiles against the Moka.Red 0.1.8 NuGet package, so every fix targets the 0.1.8
API (checked against `git show v0.1.8:` and the package metadata) and also holds for current src/:
- MokaCalloutType.Info/Success -> Note/Tip, which keep the blue/green colours (fade-in, form-builder,
  reveal, slide-in, stagger, wizard).
- date-range-picker: DateOnly? instead of DateTime?.
- theme-switcher: MokaThemeSwitcherItem records bound to MokaTheme?; the parameter table documented
  a MokaThemeOption type that never existed and was rewritten.
- transfer-list: List<string> for IList<T>, and one-way SelectedItems. The component moves items in
  place and has no SelectedItemsChanged, so @bind-SelectedItems and the Array.Empty/new[] fields
  would have thrown at runtime even once they compiled.
- infinite-carousel: MokaCarouselDirection, and MokaInfiniteCarouselSlide instead of
  MokaCarouselSlide, which never registers with an infinite carousel.
- split-pane: MokaSplitOrientation. marquee: Gap is MokaSpacingScale?. swipe-actions: Threshold is
  an int. form-builder: Options is IList<string>?, which `new()` cannot target. toggle-group: 0.1.8
  has no Content.List/Grid icons, so Navigation.Menu and Content.Image.
- Parameter tables on those pages carried the same wrong types plus wrong defaults (fade-in, slide-in,
  stagger, infinite-carousel, form-builder, date-range-picker). Corrected where verified.

A second kind of broken preview compiled cleanly. 39 namespaces holding public 0.1.8 types were
missing from the `usings` in docs/mokadocs.yaml, so MokaDropdown, MokaPopover, MokaCarousel,
MokaImage, MokaMediaGallery, MokaStat, MokaPanel, MokaStatusBar and MokaStepper rendered as literal
unknown elements on 9 pages (Razor only warns). All 39 were added after a simple-name collision
check. Resolving those tags exposed two bugs they had been hiding: a nonexistent
MokaIcons.Content.Draft on split-button, and data-list using the MokaDataListItem record as a child
component (rewritten to Items with ValueContent templates). Moka.Red.Feedback.Base and
Moka.Red.Data.Base were removed from `usings`: this session deleted both namespaces from src/, and a
using the package lacks fails every preview on the site, so the next `library:` bump would have
broken all 400.

Verified: mokadocs 1.6.0 build, 400 of 400 previews compile, no page renders a literal <Moka tag.
All 92 previews on the 23 touched pages booted in the WASM preview host with no error UI, console
error or unhandled exception. Spot checks: theme switcher selection by value, infinite carousel
registering 3 slides plus loop clones (vertical uses translateY), the dropdown opening, split pane
vertical, data-list rich rows.

Still wrong in the live preview until a release: MokaTransferList re-renders on its first click and
freezes on the second. That is the render-latch bug fixed in src/ this session (ShouldRender), and the
docs pin 0.1.8.

Docs: .claude/docs/07-build-test-release.md "Preview snippets", a gotcha in 08-gotchas.md, rule 22 in
CLAUDE.md, and a `docs-local` entry in .claude/launch.json that serves from ../Moka.Docs source.

### Also
- ClearProjectsCache.{bat,ps1,sh} were copied from Moka.Docs and still called themselves "MokaDocs",
  with em dashes that rendered as garbage in a CP437 console. Relabelled and ASCII-fied.
- Docs: CLAUDE.md (Build & Test, structure), .claude/docs/07-build-test-release.md (launcher and
  mokadocs sections).

## 2026-09-12 - mokadocs 1.7.0 and the dead sidebar config

BC released mokadocs 1.7.0 the same day. Moka.Red was checked against it with the Moka.Docs source at
tag v1.7.0 (NuGet has 1.7.0 too, and CI installs the latest).

### What 1.7.0 changed for this repo
- `build` exits 1 on errors, and plugin warnings reach the build summary. Moka.Red: 93 pages, 0 errors,
  9 warnings, exit 0. All 400 previews still compile and no page renders a dead `<Moka` tag.
- The 9 warnings are the blazor-preview noscript SSR fallback lacking IMokaToastService (toast,
  quick-start). They used to be invisible. Because of them `validate` and `doctor` exit 1.
- `validate` is now a real dry-run build. `doctor`: 11 passed, 1 warning (those 9), 0 errors; the old
  "unknown plugins" false positive is gone and relative links are checked.
- Page "Last updated" dates come from git, so docs.yml now checks out with `fetch-depth: 0`.
- Preview compile errors are still logged as warnings, so `build` exits 0 on them; `validate` lists them.

### The sidebar config never worked
mokadocs has only ever read `nav:` (label, route path, children). Moka.Red's mokadocs.yaml used
`navigation:` with `title`/`items`/`.md` paths from its first commit in April, and mokadocs drops
unknown keys silently. So the live site showed a sidebar generated from folder names ("Api",
"Getting started", the home page last), and rule 22's "add a nav entry" did nothing.
Converted the block to `nav:`. The result follows the authored config: Getting Started (Overview
first), Components, Layout (which groups six decorative components the yaml placed there), API
Reference. The 11 component entries and 3 layout entries appended that morning were moved to where
their front matter order puts them, with their page titles as labels. Verified in the built site: 93
links, every href resolves, current-page highlight works, all four sections expanded as before.
- `site.footer` was dead too; it is now `site.copyright: "MIT License"` (footer shows it next to
  "Built with MokaDocs").
- `theme.options.accent` was not a real key either. The real one, `accentColor`, sets `--color-accent`,
  which the moka-red preset leaves amber, so renaming it would have recolored the site. BC chose to
  delete the line.

### Not changed, for BC (mokadocs behaviour)
- Prev/next links sort all pages by front matter `order`, then route, ignoring the sidebar: Split
  Pane (order 23) shows Progress & Loading and Dropdown as neighbours. Same in 1.6.0.
- The SSR fallback warnings above, and unknown yaml keys passing without a warning.

### Launcher
- `RunSamples.bat docs validate` added back (menu key V; D, U, X and L renumbered). validate and doctor
  run the solution build first like build does, and print what exit 1 (warnings only) or 2 (errors)
  means. Tested: help alignment, one-shot dry runs, piped menu keys, and a real validate run.
- Corrected a comment claiming previews need the solution build. They compile against preview-host's
  own bin/Release (the pinned package) unless the plugin gets a `references:` option.

### Docs
.claude/docs/07-build-test-release.md (mokadocs section rewritten for 1.7.0, CI table, launcher table),
08-gotchas.md (silently ignored config keys), CLAUDE.md (version line, rule 22 nav schema).

## 2026-09-12 - Moka.Red 0.1.9 released

Followed BC's release notes from the previous machine (E:\NewPc\.claude) and the Moka.Docs
pattern: CHANGELOG section, `chore: release 0.1.9`, push, CI green, annotated tag v0.1.9, then the
docs preview pin in `docs: bump preview host to Moka.Red@0.1.9` once NuGet served the packages.
- BC reported chip colors not rendering on the live docs. MokaChip applied `Color` only when
  selected; fixed before the tag (`fix: show chip colors when not selected`). The dropdown and
  popover reports were the pre-deploy dead-markup pages, working after the docs deploy.
- publish-nuget.yml ends red on every release because its GitHub Packages push fails; the
  NuGet.org push succeeds. Moka.Red.Templates is not published by any workflow.

## 2026-09-13 - MokaChip variants

BC asked for chips with backgrounds and styling like the buttons.
- MokaChip honours `Variant` with MokaButton's four looks and defaults to `Soft` (tinted
  background). Filled is a tinted border that fills on hover, Outlined a full-color border, Text
  neither. Selected adds a colored fill, border and inner glow on any variant; an uncolored chip
  takes the primary color when selected.
- One private custom property (`--_chip-color`) carries the color into every variant instead of
  a rule per variant and color. Neutral chips use the surface and outline tokens so dark-theme
  borders stay red-tinted. Sizes, margins and disabled opacity moved to tokens.
- 11 bUnit tests (MokaChipTests, 382 total). DevApp Primitives page has a variants grid. The docs
  chip page documents `Variant` with an example.
- Verified computed styles for every variant, color and selected state in the dark and light
  themes, plus a headless Edge screenshot of both.
- Not released. Docs previews compile against Moka.Red 0.1.9, so the live chip page keeps the old
  look until the next release and preview-host bump.

## 2026-09-13 - Moka.Red 0.1.10 released

Same steps as 0.1.9: CHANGELOG section, `chore: release 0.1.10`, push, CI green, annotated tag
v0.1.10, then `docs: bump preview host to Moka.Red@0.1.10` once NuGet served the packages. The live
chip page showed the new variants after the docs deploy.

## 2026-09-18 - Accessibility and timing fixes (dialog, select, list item, hosts)

BC found four problems from a consumer app: MokaDialog not labelled by its title and trapping focus
late, MokaSelect with no accessible name, MokaListItem out of reach of the keyboard, and an
`async void` in MokaToastHost.
- MokaDialog: the title has an id and the box points `aria-labelledby` at it. Unmatched attributes
  and `Id` now land on the `role="dialog"` box, so a dialog without a title can take `aria-label`.
  The module is imported on first render while the dialog is closed, concurrent callers share one
  import, and the trap attaches before scroll lock and drag. Measured in the DevApp: focus is inside
  the dialog 15-24 ms after the click, first open included.
- MokaSelect: MokaFieldWrapper's label gets an id (`LabelIdFor`). The combobox and the listbox use
  `aria-labelledby`, else a consumer `aria-label`, else the placeholder. `aria-expanded` renders as a
  string, and `aria-controls` points at the listbox while it is open.
- MokaListItem: inside a MokaList, rows with OnClick or OnContextMenu get `tabindex="0"`, the
  `moka-list-item--interactive` class and a focus ring. Enter and Space are handled by a new
  `MokaList.razor.js`: one delegated listener per list, bound when the first clickable row renders,
  that calls `click()` only when the focused row itself gets the key. The first version used .NET
  keydown/keyup handlers on the row and was dropped: keys bubbling from a nested button or input
  activated the row, and Space scrolled the page.
- MokaToastHost and MokaDialogHost: the `async void` event handlers became `_ = ApplyAsync(change)`,
  which changes state on the renderer's thread and passes failures to `DispatchExceptionAsync`.
  These were the only four `async void` methods in the repo.
- Tests: 22 new, 404 total (Feedback 76, Forms 27, Primitives 108), including a reflection guard
  against `async void`. With the src changes stashed, 5 Feedback, 5 Forms and 4 Primitives tests fail.
- Checked in the DevApp (new clickable list demo on /misc): real Tab and Enter, the focus ring, the
  disabled row skipped, Space through dispatched events (the browser tool cannot send a real Space),
  keys from a nested button ignored. Select names on /forms, dialog labels and focus timing on
  /feedback.
- Docs: new components/list.md with a nav entry, accessibility notes on dialog.md and select.md, and
  a line on calling the toast and dialog services from any thread. mokadocs build: 94 pages, 0
  errors, the same 9 toast prerender warnings. The mokadocs global tool is no longer installed, so
  the build used 1.7.0 installed into the session scratchpad. Run it from PowerShell or cmd: Git
  Bash rewrites `--base-path /Moka.Red/` into a Windows path.
- Internal docs: 08-gotchas #10-13 (bool ARIA attributes, label for, key handlers on divs, async
  void) and two new open defects, 04 section 11, 06 (MokaList module, dialog preload, ScrollToTop
  as it is now), and 05's toast and dialog gotchas, which were stale since 0.1.9.
- Found, not fixed at the time: Space on MokaSelect's trigger scrolled the page, and link list items
  put `role="listitem"` on the `<a>`. Both fixed in the next entry. The same note claimed MokaTable
  cells scrolled on Space; that was wrong, `moka-table.js` `initGridKeys` already cancels it.

## 2026-09-18 - Keyboard and screen reader fixes, round two (0.1.11)

BC asked for a new version, the two open items fixed, and bugs fixed along the way. A survey of
`@onkeydown` handlers turned up the same two limits of Blazor in several components: it cannot
cancel the browser's default action for one key, and `KeyboardEventArgs` has no target.
- Core `moka-keys.js`: `bindActivation(root, selector?)` makes Enter and Space click a focused
  element the way they click a button, for the element itself only; `preventKeys(root, rules)`
  cancels chosen keys' defaults and still delivers them. `moka-drag.js` and
  `MokaContextMenu.razor.js` re-export them. `SafeModuleInvokeVoidAsync` on MokaComponentBase and
  MokaInputBase wraps the module call. Rules travel as `Dictionary<string, object?>[]` because
  trimming can strip anonymous types in WebAssembly apps.
- MokaList moved onto `bindActivation` and `MokaList.razor.js` was deleted. Link rows wrap the `<a>`
  in a `role="listitem"` div so the link keeps its role, and a disabled link keeps `role="link"`.
- MokaSelect: Space and the arrows no longer scroll the page, and Enter in the search box no longer
  submits a form. The search box was never focused on open (`_searchInputRef` was unused) and Tab
  from the trigger closed the list, so keyboard users could not search; focus now moves in on open,
  Enter and Escape hand it back, Tab closes. `aria-activedescendant`, option ids, `aria-selected`
  as strings, the listbox role moved off the container so the search box is outside it, named
  groups. Under GroupBy the arrows walked `Items` order while the options showed grouped; they now
  walk `DisplayItems`. Enter on the clear or chip buttons also opened the list (bubbling); the
  buttons flag the key and the trigger skips it. A disabled select was focusable and its clear and
  chip buttons still worked.
- MokaAutoComplete: Enter picking a suggestion submitted a surrounding form; now cancelled only
  while one is highlighted. `aria-expanded` string, `aria-controls`, labelled listbox,
  `aria-activedescendant`, `aria-selected` on the highlighted option.
- MokaChat: `@onkeydown:preventDefault="@(_inputText.Trim().Length > 0)"` with `ShouldRender => true`
  and `@bind:event="oninput"` cancelled every key once the box held text, so typing stopped after one
  character. Plain Enter is cancelled in JS now, Shift+Enter adds a line, IME Enter does not send.
- MokaContextMenu: nav keys cancelled (rebound on every open, the element is new each time), items
  have ids and the menu has `aria-activedescendant`.
- MokaBentoItem, MokaGlassCard: the .NET Enter/Space handler on the card also fired for keys in
  controls inside it, and Space scrolled; replaced by `bindActivation`. Static cards lost
  `tabindex="-1"`. Clickable cards got the focus ring.
- A mistake caught in the browser: the first MokaSelect rewrite rendered options with a Razor local
  function. It wrote to MokaSelect's own builder while being called inside MokaFieldWrapper's
  ChildContent, so the browser failed to apply the render batch and the circuit died. bUnit tests
  still passed because they only looked for the options anywhere. Fixed with a template
  (`OptionTemplate`), plus a test that the options sit inside the listbox, which fails on the
  broken version. Recorded as gotcha #14.
- Tests: 427 (Primitives 113, Forms 41, Layout 24). With the src changes stashed, 17 Forms, 9
  Primitives and 3 Layout tests fail, including the clear-button and chat-typing tests, which
  confirms bUnit bubbles keydown and renders the preventDefault flag.
- Checked live in the DevApp: list rows, select (all seven demos), autocomplete with real typing,
  chat with real typing and send, context menu, bento and glass cards with real Tab. Space and some
  defaults through dispatched events, because the browser tool cannot send a real Space key.
- Docs: new components/chat.md (MokaChat had no page) with a nav entry; select, list, forms
  (AutoComplete), bento-grid and glass-card updated. mokadocs 1.7.0 (scratchpad copy): 95 pages,
  406 previews compiled, 0 errors, the same 9 warnings.
- Found, not fixed: about fifteen mouse-only click targets (radio items, rating, tree, menus,
  dropdown items, clickable cards, avatars, steps, notifications, gallery, theme switcher, virtual
  list, table row click, tab strip Enter/Space), and Escape inside a dialog closing both the popup
  and the dialog. Listed in CLAUDE.md known issues #10 and #11.
